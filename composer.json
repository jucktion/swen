{
  "name": "swen/news-heroku",
  "description": "PHP news with VueJS on Heroku",
    "scripts": {
      "post-install-cmd": [
        "chmod -R +X temp"
      ]
    },
    "require": {
      "php": "^7.4.0"
    }
}